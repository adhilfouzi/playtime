import 'package:flutter/material.dart';

class AllTrufList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('All Truf List'),
    );
  }
}
