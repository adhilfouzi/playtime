import 'package:flutter/widgets.dart';

class CompletedBooking extends StatelessWidget {
  const CompletedBooking({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Completed Booking'),
    );
  }
}
